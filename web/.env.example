# Convex deployment URL — set in Replit Secrets, not committed.
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Optional: hardcode a project ID so the app boots straight into it.
# If unset, the app fetches the first project from api.projects.listProjects.
VITE_CURATE_MIND_PROJECT_ID=
