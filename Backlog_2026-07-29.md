# Curate Mind Backlog, July 29, 2026

Three horizons, built from the operating review (`Curate_Mind_Operating_Review_2026-07-29.md`):

- **Now:** small quality-of-life work. No big decisions required. Each item has a paste-ready prompt for a fresh Claude or Codex chat.
- **Next:** the work that drives toward the north star (curator cockpit, in-Claude answer parity, living report, mechanical automation). Items note where a decision from you gates the work.
- **Later / Retire:** items from existing documentation that the new direction supersedes or that were already quietly completed.

---

## Now: quality-of-life fixes

### N1. Citations without asking (minimal answer contract)

The top experience fix: stop having to ask for citations in Cowork.

```
In the Curate Mind repo, make cm_ask answers carry their own formatting contract so any client model produces cited answers without being asked. Two changes, no schema migration:
1. In the MCP server, add a `renderContract` field to the cm_ask response containing the locked rules already used by the website composer in convex/chat.ts (the [E#] inline citation rules and evidence ordering; see buildGroundedSystemPrompt around line 824). Also state the contract in the cm_ask tool description.
2. Update skills/cm-workflow-router/SKILL.md so the "ask the corpus" path instructs the model to follow renderContract exactly: stance first, [E#] inline citations, source reference list with anchor quotes at the end.
Do not change the website. Run existing MCP tests if present.
```

### N2. Register cm_correct_claim

The claim-correction tool from Design Decision 37 is missing from `ALL_TOOLS` in `mcp/src/toolsets.ts`, so it is unreachable in every toolset. A background task chip for this was already created on July 29 (a session may already be running); check its status before starting.

```
In the Curate Mind repo: cm_correct_claim is registered in mcp/src/tools/synthesis.ts but absent from ALL_TOOLS in mcp/src/toolsets.ts, so the toolset gating hides it in every toolset including "all". Add it to ALL_TOOLS (and the appropriate toolsets alongside cm_correct_anchor and cm_correct_attribution), and add a guard test that fails if any registered tool name is missing from ALL_TOOLS so this cannot recur.
```

### N3. Fix silent truncations and short IDs

The cause of every validation-error storm since May.

```
In the Curate Mind repo, fix three related MCP data-integrity issues:
1. cm_get_data_points_batch silently clips at roughly 25 records. Add explicit pagination or chunked responses with a clear "truncated: true, next cursor" signal instead of silent clipping.
2. cm_list_data_points_by_source display-truncates for sources with more than about 33 data points, making later data point IDs unreachable. Same fix: paginate or signal truncation explicitly.
3. Audit the batch orchestrator and flag-report generation in skills/ so every generated report always carries full Convex IDs, never 8-character abbreviations. Abbreviated IDs in handoff documents caused repeated ArgumentValidationError storms.
Add or update tests for the pagination behavior.
```

### N4. Extend capturedAt to URL and YouTube intake

Finishes what the July 14 pull request started for PDFs and ends the folder-moving chore.

```
In the Curate Mind repo, extend the optional capturedAt parameter (added to cm_extract_pdf in PR #1) to cm_fetch_url and cm_fetch_youtube, so sources captured earlier but processed later get filed into the capture week's folder instead of the processing week. Follow the same convention and update the week-folder documentation in CLAUDE.md and docs/source-intake-guide.md. Remember to run npm run agents:sync and npm run agents:check after editing CLAUDE.md.
```

### N5. Reconcile the review trackers

Makes the review queue tell the truth (currently reports ~220 pending; the real number is ~75-80).

```
In the Curate Mind repo, reconcile sources/*/review-status.json with reality. Use cm_list_sources (Convex is the source of truth) to determine what is actually ingested. Then:
1. The four February week folders (sources/2026-02/) have no review-status.json but their ~140 files were ingested in the March 22-23 backfill. Create trackers marking them ingested.
2. sources/2026-07/2026-07-05_to_11/review-status.json exists but is empty ({"ingested": []}); the July weeks 07-12_to_18, 07-19_to_25, and 07-26_to_08-01 have no tracker. Create accurate trackers (these files are genuinely pending).
3. The 2026-05-31_to_06-06 tracker has entries referencing files no longer on disk; flag them for my review, do not delete entries silently.
Report before/after pending counts. Do not ingest anything.
```

### N6. Extract the 13 orphaned June sources

Ingested June 2-3, never extracted, invisible to positions and search for 8 weeks.

```
In Curate Mind, 13 sources ingested around June 2-3 have status "indexed" and zero data points (including "Expanding Project Glasswing", "The Eight Levels of AI Adoption", "The AI Token Shortage Begins", "Your prompts are tech debt", the Uber AI-budget pair). Use cm_list_sources to find all indexed-not-extracted sources, confirm the list with me, then run the standard batch extraction pipeline on them (Extract, Secondary Capture, Enrich) per skills/cm-batch-orchestrator. Produce the normal flag report at the end for my review.
```

### N7. Resolve the stuck July 11 PDF

```
In sources/2026-07/2026-07-05_to_11/ there is verify_better-up-uplift-the-hidden-cost-of-workslop_2026-07-11.md with all four metadata fields still [verify] placeholders and mangled OCR text, plus a FAILED- sibling from the same minute. Re-run cm_extract_pdf on the original BetterUp/Stanford "workslop" PDF in that folder (the OCR stack was stabilized June 18, after this failure). If extraction succeeds, help me fill the metadata header so it is ready for ingestion; if it still fails, mark it clearly and delete nothing.
```

### N8. Repair failed-source anomalies

```
In Curate Mind (Convex), two append-only lineage repairs, following Design Decision 38. Deletes are forbidden:
1. Source kd75bhcy... ("Predicting AI job exposure", Benedict Evans) has status "failed" but still has live, position-linked data points, while its June 2 replacement kd751s2e... also exists. Audit which data points are linked where, migrate evidence links to the replacement's data points where they correspond (cm_replace_evidence_on_position), supersede the stale ones (cm_supersede_data_point), and link the sources with cm_supersede_source.
2. The four arXiv LLM-simulation papers each appear 2-3 times in the failed-source list from duplicate re-ingests. Verify each failed twin has no live evidence links and record supersede lineage where missing.
Show me the audit before making any writes.
```

### N9. Quick tag merge pass

Only the obvious merges; the full taxonomy overhaul is a Next item.

```
In Curate Mind, do a conservative tag cleanup pass. Using cm_get_tag_trends and the tag tools:
1. Merge exact-concept duplicates: capitalization variants (human-in-the-loop), "Trust" / "Trust in AI" / "Trust and Verification", "Workflow Moat" / "Workflow Data Moat", and any other near-identical pairs you find. Merging means re-tagging data points to the surviving tag (cm_update_data_points_tags_batch, cm_remove_data_point_tag_batch) and retiring the duplicate (cm_retire_tag).
2. Retire the ~40 zero-use tags (most created July 7).
Propose the merge list to me first as a table (survivor, retired, data point count); execute only after I approve. Do not restructure categories in this pass.
```

### N10. Folder hygiene

```
Clean up filesystem cruft in the Curate Mind sources folders (files only, never touch Convex):
1. sources/2026-07/2026-07-05_to_11/ contains 38 orphaned .fuse_hidden* files and a stray file named xxpnrdiq from a cloud-sync mount. List them for my confirmation, then remove them.
2. sources/2026-05/2026-05-10_to_16/ contains non-source artifacts (Pass1/Pass2/Pass4 reports, _decisions.md, test_verify_header_guard.md, one FAILED-verify file). Propose moving them to a _artifacts subfolder so directory scans see only sources; move after I confirm.
```

### N11. Documentation truth pass

```
In the Curate Mind repo, the documented MCP toolset counts are stale: Architecture_Spec.md and docs/mcp-tool-inventory.md say daily 25 / pipeline 44 / admin 52 / all 52, but mcp/src/toolsets.ts actually has 27 / 54 / 59 / 59. Regenerate the tool inventory from the code (ideally add a small script so docs can be regenerated instead of hand-counted), update both docs, and check for other drifted counts. If CLAUDE.md changes, run npm run agents:sync and npm run agents:check.
```

### N12. Verify skill registration in Cowork

```
In past Curate Mind pipeline runs, invoking cm-batch-orchestrator sometimes failed with "Unknown skill" and the model fell back to reading SKILL.md manually. Investigate how skills/ are registered in my Cowork environment for this project, verify all five skills (cm-workflow-router, cm-batch-orchestrator, cm-curator-review, cm-evidence-linker, cm-deep-extract) resolve correctly, and fix or document whatever registration step is missing so pipeline prompts can rely on skill invocation.
```

### N13. Give the CoP deliverables a home

Smallest possible decision, with a default: the three hand-versioned narrative files at the repo root (`AI-CoP-Video1-Narrative-v3.md`, `AI-CoP-WhatIsIt-v2.md`, `CoP_Video1_Topic1_Narrative.md`) move to a gitignored `deliverables/` folder. They are generated outputs whose source of truth is the corpus plus the Google Doc, so they should not sit as loose root files. If you would rather version them in git, say so and track the folder instead.

---

## Next: driving toward the north star

Ordered roughly by dependency. Items marked **[decision]** need a call from you before build.

1. **Pipeline state in Convex.** A pipelineRuns record per week (stage, wave, pending IDs, outstanding flags). Every session opens with one standing "continue processing" prompt that reads state. Kills the hand-carried handoff prompt. Prerequisite for the cockpit.
2. **Auto-advancing waves with checkpoints and rate-limit auto-resume.** Replaces the "go" gates; formalizes the resume protocol hand-built in May.
3. **Clear the real backlog.** Roughly 75-80 files, about three batch cycles at July throughput. Start with the July 5-11 model-release week (37 files, highest value). Can begin any time; gets cheaper after items 1-2.
4. **Scheduled week close.** Embedding drain plus Research Lens regeneration triggered after integration instead of via one more chat.
5. **Ballot-format flag review.** Flag reports arrive as a numbered ballot answerable in one message. Chat-first; becomes a cockpit view later.
6. **Daily Discovery watchlist.** watchedSources and intakeCandidates in Convex; a scheduled agent checks YouTube channel RSS feeds and blog RSS via Supadata, sends a digest, you approve by ballot, approved items are fetched into the correct capture week. **[decision]** newsletter path: Gmail connector or a newsletter-to-RSS bridge.
7. **Ask skill: the website experience inside Claude.** Enforces the locked answer contract (built on N1) and renders the site-style evidence panel inline in Cowork, composed on your subscription.
8. **Public MCP with server-composed answers.** Same composer as the site, returned through the invite-gated public MCP for guaranteed parity in any client. **[decision]** usage caps or bring-your-own-key before any open distribution, since server composition bills your API key.
9. **Curator cockpit.** Intake inbox, pipeline status, position health; decide-in-UI, execute-in-Cowork. **[decision]** fold into the planned April frontend re-architecture or advance ahead of it.
10. **Living report view.** Report-style public front page over current positions with a "what changed" feed derived from position versions and change summaries. Generated on load; no maintained document.
11. **Full tag taxonomy consolidation.** Beyond N9: collapse the 36 category schemes to a handful, define a tagging guideline for Enrich, shrink the singleton tail. Makes cm_get_tag_trends a real trend instrument.
12. **Post-cycle "what changed" brief.** Generated after each integration wave (Lens diff, new and re-versioned positions, tag movers); delivered, never stored.

---

## Later / Retire: documentation superseded by the new direction

| Item | Where it lives | Status |
|---|---|---|
| "Phase 2: Reprocess February" milestone | Architecture_Spec.md | Effectively complete: February was ingested in the March 22-23 backfill. Mark done once N5 reconciles the trackers. |
| Four-layer Reader/Analyst access matrix remnants | Older doc sections | Already deprecated by Decision 34 (Stance/Evidence/Source). Scrub remaining references during the N11 docs pass or later. |
| Three-chat workflow formalization (May 11) | Skills, workflow docs | Superseded once pipeline state lands (Next 1-2): the chat-handoff protocol is exactly what gets automated away. Update docs then, not before. |
| Project-profile wiring placeholders | All four pipeline skills | Parked since May 20. Matters for open-source clones, not for the north star. Either land it or remove the placeholder tables when clones become a priority. |
| Dispatch draft intake tool | Mobile intake flow | Its wrong-date folder bug gets superseded by N4 plus the Daily Discovery inbox (Next 6). Retire the workaround notes afterward. |
| Suggested prompts in the project profile | Project profile | Unused in practice. Fold into the ask skill and a scheduled monthly deep question (Next 7) instead of maintaining as profile copy. |
| WorkspacePage.tsx reserved file | web/src/pages/ | Keep reserved; decide its fate during the cockpit build (Next 9), not before. |
| "Weekly" cadence remnants | Folder convention, Lens trigger docs | Decision 36 removed weekly language, but the new direction reintroduces scheduled operations. Reconcile docs to "batch-triggered with a scheduled close" when Next 4 lands. |
| CRIS reference paths | CLAUDE.md, misc docs | Archive-only; trim references whenever docs are next touched. |
